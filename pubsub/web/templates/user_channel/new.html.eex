<h2>New user channel</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_channel_path(@conn, :create) %>

<span><%= link "Back", to: user_channel_path(@conn, :index) %></span>
