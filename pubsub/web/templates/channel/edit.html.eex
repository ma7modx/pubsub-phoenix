<h2>Edit channel</h2>

<%= render "form.html", changeset: @changeset,
                        action: channel_path(@conn, :update, @channel) %>

<span><%= link "Back", to: channel_path(@conn, :index) %></span>
